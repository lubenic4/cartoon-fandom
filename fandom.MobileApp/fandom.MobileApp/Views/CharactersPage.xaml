<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewModels="clr-namespace:fandom.MobileApp.ViewModels"
             x:Class="fandom.MobileApp.Views.CharactersPage">
    <ContentPage.BindingContext>
        <viewModels:CharactersViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemTapped="OnTap" SelectionMode="None" ItemsSource="{Binding characters}" Header="List of characters">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Vertical">
                                <StackLayout Orientation="Horizontal">
                                   <Image HeightRequest="50" WidthRequest="60" Source="Assets/southparklogo.png" />
                                   <StackLayout Orientation="Vertical">
                                    <Label Text="{Binding FirstName}"/>
                                    <Label Text="{Binding Id}"/>
                                   </StackLayout>
                                </StackLayout>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>